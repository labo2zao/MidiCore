# MidiCore Configuration - Minimal Setup
# Use this for quick testing with minimal hardware
# Only 7 supported parameters (matches config_io parser)

SRIO_DIN_ENABLE = 1
SRIO_DIN_BYTES = 1
DIN_INVERT_DEFAULT = 0

AINSER_ENABLE = 0
AINSER_SCAN_MS = 5

AIN_VELOCITY_ENABLE = 0
AIN_CALIBRATE_AUTO = 0
